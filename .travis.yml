language: haxe

sudo: false

haxe:
  - "3.4.7"
  - development

matrix:
  allow_failures:
    - haxe: development

install:
  - haxelib install hxnodejs
  - haxelib dev hxnodejs-sqlite3 .

script:
  - haxe haxedoc.hxml
  - haxe --cwd example/ build.hxml
